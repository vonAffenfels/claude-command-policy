---
description: Migrate shfmt-permissions.json to command-policy.json
---

Invoke the **`command-policy:migrate-config`** skill (Skill tool, `skill: "command-policy:migrate-config"`) and work
through it. It holds the full procedure - the dry run, presenting warnings and the diff, confirming, and applying; don't
reimplement any of that here.

Arguments: `$ARGUMENTS`
